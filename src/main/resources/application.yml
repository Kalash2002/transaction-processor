spring:
  application:
    name: transaction-processor

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/transactions}
    username: ${DB_USER:transactions}
    password: ${DB_PASSWORD:transactions}

banking:
  max-transaction-amount: 1000000000.00
  default-page-size: 20
  max-page-size: 100

server:
  port: 8080
